{
  "label": "ART",
  "description": "Optionally select which quarter of data to use at time point 1 and 2",
  "controlGroups": [
    {
      "label": "Number on ART",
      "controls": [
        {
          "name": "art_calendar_quarter1",
          "label": "Calendar quarter midpoint of survey",
          "type": "select",
          "helpText": "Select calendar quarter of input ART data at the midpoint of survey data",
          "options":
            <art_calendar_quarter1_options>,
          "required": false
        },
        {
          "name": "art_calendar_quarter2",
          "label": "Calendar quarter to generate estimates",
          "type": "select",
          "helpText": "Select calendar quarter of input ART data at which to generate estimates",
          "options":
            <art_calendar_quarter2_options>,
          "required": false
        }
      ]
    }
  ]
}
